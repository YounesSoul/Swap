# Swap — Student Skill Exchange

Monorepo: Next.js frontend (`swap-frontend`) + NestJS backend (`swap-backend-pg`).

## Local dev
- Backend: `cd swap-backend-pg && npm i && npm run dev`
- Frontend: `cd swap-frontend && npm i && npm run dev`
